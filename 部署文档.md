# MagicCode 部署文档

> 本文档记录了将 MagicCode 项目从本地部署到阿里云服务器的完整过程，可作为以后部署任何 Java 项目的参考模板。

---

## 一、环境信息

| 项目 | 信息 |
|------|------|
| 服务器 | 阿里云轻量应用服务器 |
| 公网 IP | 47.116.76.13 |
| 操作系统 | Ubuntu 22.04 LTS |
| Java 版本 | OpenJDK 21 |
| 服务端口 | 18888 |
| 访问地址 | http://47.116.76.13:18888 |

---

## 二、部署流程总览

```
本地打包 jar ──→ scp 上传到服务器 ──→ SSH 登录服务器
──→ 前台试运行测试 ──→ 阿里云开放端口 ──→ 配置 systemd 后台常驻
```

---

## 三、详细步骤

### 第 1 步：本地打包 jar

在本地用 Maven 将项目打包成可执行的 fat jar。

```powershell
# 打开 PowerShell
cd D:\mycode\Magiccode
$env:MAVEN_HOME = "C:\Users\李现\tools\apache-maven-3.9.9"
& "$env:MAVEN_HOME\bin\mvn.cmd" clean package -DskipTests
```

打包成功后，jar 文件在：

```
D:\mycode\Magiccode\target\magiccode-1.0.0-shaded.jar
```

**命令解释：**

| 命令 | 含义 |
|------|------|
| `mvn` | Maven 构建工具 |
| `clean` | 清理之前的编译缓存（target 目录） |
| `package` | 编译 + 打包，生成 jar 文件 |
| `-DskipTests` | 跳过单元测试（加快打包速度） |

---

### 第 2 步：上传 jar 包到服务器（scp）

```powershell
# 上传 jar 包
scp D:\mycode\Magiccode\target\magiccode-1.0.0-shaded.jar root@47.116.76.13:/root/
```

```powershell
# 上传配置文件夹（-r 表示递归上传整个文件夹）
scp -r D:\mycode\Magiccode\.magiccode root@47.116.76.13:/root/
```

**执行过程：**
1. 第一次连接会问 `Are you sure you want to continue connecting?` → 输入 `yes`
2. 提示 `password:` → 输入服务器密码（屏幕不显示，正常现象）
3. 看到进度条到 100% 说明上传完成

**命令格式：**

```
scp [选项] 本地文件路径 用户名@服务器IP:远程存放路径
```

| 参数 | 含义 |
|------|------|
| `scp` | Secure Copy，通过 SSH 安全复制文件 |
| `-r` | recursive，递归上传整个文件夹（传单个文件不需要加） |
| `root@47.116.76.13` | 用户名 @ 服务器公网 IP |
| `:/root/` | 上传到服务器的目标目录 |

**为什么需要传两个东西？**
- `jar 包` = 程序本体（代码 + 依赖）
- `.magiccode/config.yaml` = 配置文件（API Key、模型选择等）
- 类比：游戏本体 + 存档文件，缺一不可

---

### 第 3 步：SSH 登录服务器

```bash
ssh root@47.116.76.13
```

登录后验证文件是否到位：

```bash
# 查看 jar 文件
ls -lh /root/magiccode-1.0.0-shaded.jar

# 查看配置文件
cat /root/.magiccode/config.yaml
```

**命令解释：**

| 命令 | 含义 |
|------|------|
| `ssh root@IP` | 以 root 用户远程登录服务器 |
| `ls -lh 文件` | 查看文件详情（`-l` 详细信息，`-h` 人类可读大小） |
| `cat 文件` | 查看文件内容 |

---

### 第 4 步：前台试运行

先在前台运行，确认能正常启动，有报错能第一时间看到。

```bash
java -jar /root/magiccode-1.0.0-shaded.jar --remote
```

**成功标志：**

```
Started MagicCode in x.xxx seconds
Remote UI: http://localhost:18888
```

**命令解释：**

| 参数 | 含义 |
|------|------|
| `java` | 运行 Java 程序 |
| `-jar` | 指定运行一个 jar 包 |
| `--remote` | 项目启动参数，开启远程 Web 服务（端口 18888） |

> ⚠️ 前台运行时终端会被占住，按 Ctrl+C 会停止服务。

---

### 第 5 步：阿里云开放端口

阿里云安全组默认关闭所有端口，需要手动放行 18888。

**操作步骤：**

1. 打开阿里云控制台 → 找到服务器实例
2. 点进去 → 左侧菜单「安全组」
3. 点「配置规则」→「手动添加」
4. 填写规则：

| 字段 | 值 |
|------|-----|
| 方向 | 入方向 |
| 授权策略 | 允许 |
| 协议类型 | 自定义 TCP |
| 端口范围 | 18888/18888 |
| 授权对象 | 0.0.0.0/0 |
| 描述 | MagicCode |

5. 点保存

**安全组是什么？**
> 相当于服务器的"门卫"，默认所有端口关闭，需要手动告诉门卫哪些端口允许外部访问。

**Connection refused vs 超时的区别：**

| 现象 | 含义 |
|------|------|
| 连接超时 | 安全组没放行（门没开） |
| Connection refused | 端口放行了但服务没启动（门开了，房间里没人） |

---

### 第 6 步：配置 systemd 后台常驻

前台运行的问题：终端关闭/断开就停止，服务器重启不会自动恢复。

#### 6.1 先停掉前台运行的服务

在终端按 `Ctrl + C`，确认进程退出。

> ⚠️ 重要：确保旧进程彻底退出！可以用以下命令检查：
> ```bash
> ps aux | grep java | grep -v grep
> ```
> 如果还有残留进程，用 `pkill -f magiccode` 杀掉。

#### 6.2 创建 systemd 服务文件

```bash
cat > /etc/systemd/system/magiccode.service << 'EOF'
[Unit]
Description=MagicCode AI Assistant
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/java -jar /root/magiccode-1.0.0-shaded.jar --remote
WorkingDirectory=/root
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF
```

**服务文件每行含义：**

| 行 | 含义 |
|----|------|
| `[Unit]` Description | 服务描述 |
| `After=network.target` | 等网络就绪后再启动 |
| `Type=simple` | 简单类型，主进程就是服务进程 |
| `ExecStart=...` | 启动命令 |
| `WorkingDirectory=/root` | 工作目录（配置文件在这里找） |
| `Restart=always` | 崩溃后自动重启 |
| `RestartSec=5` | 重启前等待 5 秒 |
| `WantedBy=multi-user.target` | 开机自启标记 |

**heredoc 语法解释：**
> `cat > 文件 << 'EOF' ... EOF` 是一种批量写入文件的方式。
> 从 `EOF` 之间的所有内容都会写入文件，不用手动一行行敲。

#### 6.3 启动服务并设置开机自启

```bash
# 重新加载 systemd 配置（每次新建/修改 service 文件后都要执行）
systemctl daemon-reload

# 设置开机自启
systemctl enable magiccode

# 立即启动服务
systemctl start magiccode
```

#### 6.4 检查服务状态

```bash
systemctl status magiccode
```

看到 `active (running)` 绿色字样说明成功。

---

## 四、常用运维命令

### 服务管理

```bash
# 启动服务
systemctl start magiccode

# 停止服务
systemctl stop magiccode

# 重启服务
systemctl restart magiccode

# 查看服务状态
systemctl status magiccode

# 查看服务日志（最近 50 行）
journalctl -u magiccode -n 50 --no-pager

# 实时查看日志（类似 tail -f）
journalctl -u magiccode -f

# 取消开机自启
systemctl disable magiccode
```

### 进程排查

```bash
# 查看是否有残留的 Java 进程
ps aux | grep java | grep -v grep

# 杀掉残留进程
pkill -f magiccode

# 查看端口占用
ss -tlnp | grep 18888
```

### 更新代码后重新部署

```powershell
# 1. 本地重新打包
cd D:\mycode\Magiccode
$env:MAVEN_HOME = "C:\Users\李现\tools\apache-maven-3.9.9"
& "$env:MAVEN_HOME\bin\mvn.cmd" clean package -DskipTests

# 2. 上传新 jar（覆盖旧文件）
scp D:\mycode\Magiccode\target\magiccode-1.0.0-shaded.jar root@47.116.76.13:/root/

# 3. SSH 登录后重启服务
ssh root@47.116.76.13
systemctl restart magiccode
systemctl status magiccode
```

---

## 五、部署流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        本地电脑 (Windows)                         │
│                                                                 │
│  ① mvn clean package -DskipTests                                │
│     └→ 生成 magiccode-1.0.0-shaded.jar                          │
│                                                                 │
│  ② scp jar包 + 配置文件夹 ──────────────┐                       │
│     scp -r .magiccode ─────────────────┤                        │
└─────────────────────────────────────────┼───────────────────────┘
                                          │
                                    SSH 通道 (加密)
                                          │
┌─────────────────────────────────────────▼───────────────────────┐
│                    阿里云服务器 (Ubuntu)                          │
│                                                         47.116.76.13
│  ③ java -jar magiccode.jar --remote  ← 前台试运行                 │
│                                                                 │
│  ④ 安全组放行 18888 端口                                         │
│                                                                 │
│  ⑤ systemd 配置后台常驻                                          │
│     ┌──────────────────────────────┐                             │
│     │ magiccode.service            │                             │
│     │  ExecStart: java -jar ...    │                             │
│     │  Restart=always              │  ← 崩溃自动重启              │
│     │  WantedBy=multi-user.target  │  ← 开机自启                 │
│     └──────────────────────────────┘                             │
│              │                                                  │
│              ▼                                                  │
│     systemctl daemon-reload                                     │
│     systemctl enable magiccode                                  │
│     systemctl start magiccode                                   │
│              │                                                  │
│              ▼                                                  │
│     ● active (running)  ← 24小时在线                             │
└─────────────────────────────────────────────────────────────────┘
              │
              ▼
     http://47.116.76.13:18888  ← 外网可访问
```

---

## 六、常见问题排查

### Q1：systemctl status 显示 FAILURE

最常见原因是端口被占用（之前的进程没退出干净）。

```bash
# 停掉服务
systemctl stop magiccode

# 杀掉所有残留 Java 进程
pkill -f magiccode

# 确认端口已释放
ss -tlnp | grep 18888
# 应该没有任何输出，说明端口已释放

# 重新启动
systemctl start magiccode
systemctl status magiccode
```

### Q2：外网访问不了（连接超时）

安全组没放行端口。去阿里云控制台 → 安全组 → 添加 18888 端口的入方向规则。

### Q3：外网访问不了（Connection refused）

端口放行了但服务没启动。

```bash
systemctl status magiccode   # 检查服务状态
journalctl -u magiccode -n 50 --no-pager  # 查看报错日志
```

### Q4：提示找不到配置文件

```bash
# 检查配置文件是否存在
cat /root/.magiccode/config.yaml

# 如果不存在，重新上传
# 在本地 PowerShell 运行：
scp -r D:\mycode\Magiccode\.magiccode root@47.116.76.13:/root/
```

### Q5：服务器重启后服务没自动启动

```bash
# 检查是否设置了开机自启
systemctl is-enabled magiccode
# 应该输出 "enabled"

# 如果输出 "disabled"，重新设置
systemctl enable magiccode
```

### Q6：内存不足导致服务被杀

```bash
# 查看服务器内存
free -h

# 查看服务内存占用
systemctl status magiccode

# 如果内存紧张，可以在 ExecStart 中加 JVM 参数限制内存：
# ExecStart=/usr/bin/java -Xmx256m -jar /root/magiccode-1.0.0-shaded.jar --remote
```

---

## 七、关键信息速查

| 项目 | 值 |
|------|-----|
| 服务器 IP | 47.116.76.13 |
| SSH 用户名 | root |
| SSH 密码 | （见阿里云控制台） |
| 服务端口 | 18888 |
| jar 路径（服务器） | /root/magiccode-1.0.0-shaded.jar |
| 配置路径（服务器） | /root/.magiccode/config.yaml |
| 服务名 | magiccode |
| 服务文件 | /etc/systemd/system/magiccode.service |
| 本地项目路径 | D:\mycode\Magiccode |
| Maven 路径 | C:\Users\李现\tools\apache-maven-3.9.9 |
| 访问地址 | http://47.116.76.13:18888 |

---

## 八、学到的核心技能

| 技能 | 命令/操作 | 适用场景 |
|------|----------|---------|
| Maven 打包 | `mvn clean package -DskipTests` | 任何 Java 项目 |
| 文件上传 | `scp 文件 root@IP:路径` | 本地文件传到服务器 |
| 文件夹上传 | `scp -r 文件夹 root@IP:路径` | 传整个目录 |
| 远程登录 | `ssh root@IP` | 登录服务器操作 |
| 前台试运行 | `java -jar xxx.jar` | 测试程序能否运行 |
| 端口放行 | 阿里云安全组配置 | 让外网访问服务 |
| 后台常驻 | `systemctl enable + start` | 24小时在线 |
| 查看状态 | `systemctl status 服务名` | 排查服务问题 |
| 查看日志 | `journalctl -u 服务名 -n 50` | 看报错信息 |
| 进程排查 | `ps aux \| grep java` | 找残留进程 |
| 杀进程 | `pkill -f 进程名` | 清理残留进程 |
| 端口检查 | `ss -tlnp \| grep 端口` | 看端口是否被占用 |

> 这套流程以后部署任何 Java 项目都通用，只是 jar 包名、端口、配置文件不同而已。
