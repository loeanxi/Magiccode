# MagicCode 项目说明书

> **版本：** v1.0.0 | **语言：** Java 21 | **许可证：** 私有
> **定位：** 1:1 复刻 MewCode（Go）的 AI 编程助手，利用 Java 21 现代语言特性重新实现

MagicCode 是一个基于 Java 21 的 AI 编程助手，采用 Agentic Loop 架构，支持交互式终端（TUI）、非交互打印（Print）和远程 Web（Remote）三种运行模式。项目从 Go 语言版本完整移植，在保留全部功能的同时，充分利用 Java 21 的虚拟线程（Virtual Threads）、密封接口（Sealed Interfaces）、Record、模式匹配（Pattern Matching）等现代语言特性进行重新设计。

项目包含 **133 个源文件**，分布在 **20+ 个包**中，涵盖 LLM 协议适配、多 Agent 协作、权限安全、上下文管理、TUI 渲染、技能系统、记忆系统等完整功能模块。

---

## 一、技术栈与依赖

| 领域 | 技术选型 | 版本 | 选型理由 |
|------|---------|------|---------|
| 语言 | Java（JDK 21+） | 21 | 虚拟线程、Record、密封接口、模式匹配、文本块 |
| 构建 | Maven + maven-shade-plugin（Fat JAR） | 3.9.x / 3.5.1 | 零依赖部署，单 JAR 运行 |
| 终端 UI | JLine3 + Mordant + 自研 TEA 框架 | 3.28.0 / 3.0.2 | JLine 提供终端原语，Mordant 提供颜色/Markdown 渲染，TEA 提供架构 |
| LLM SDK | Anthropic Java SDK + OpenAI Java SDK | 2.34.0 / 4.37.0 | 官方 SDK，流式支持完善 |
| Web 服务 | Spring Boot 3.x（内嵌 Tomcat） | 3.3.5 | HTTP + WebSocket 双协议 |
| MCP | 官方 Java SDK（Model Context Protocol） | 1.1.3 | 标准化工具扩展协议 |
| 配置 | SnakeYAML + Jackson | 2.2 | snake_case YAML → camelCase Java 自动映射 |
| 测试 | JUnit 5 | 5.10.x | 104 个测试用例 |
| 日志 | SLF4J NOP（静默） | 2.0.16 | TUI 场景避免日志干扰终端输出 |

---

## 二、整体架构

### 2.1 系统全景图

```
                          ┌──────────────────┐
                          │  MagicCode.java  │  入口 / CLI 解析 / 模式分发
                          └────────┬─────────┘
                                   │
                 ┌─────────────────┼─────────────────┐
                 │                 │                  │
          ┌──────▼──────┐  ┌──────▼────────┐  ┌──────▼────────┐
          │  PrintMode  │  │ MagicCodeModel│  │ RemoteServer  │
          │  (非交互)   │  │ (TUI/TEA)     │  │ (Spring Boot  │
          └─────────────┘  └───────┬───────┘  │  HTTP + WS)   │
                                   │           └──────┬────────┘
                                   └────────────────┘
                                            │
                                   ┌────────▼────────┐
                                   │    Agent.java   │  核心 Agent 循环
                                   │  · LLM 流式调用  │
                                   │  · 工具调度      │
                                   │  · 错误恢复      │
                                   │  · 自动压缩      │
                                   └────────┬────────
                                            │
              ┌──────────┬──────────┬───────┼───────────┬──────────┐
              │          │          │       │           │          │
       ┌──────▼───┐ ┌────▼─────┐ ┌─▼────┐ ┌▼────────┐ ┌▼───────┐ ▼──────────┐
       │ToolRegist│ │Streaming │ │Permis│ │Conversa-│ │  MCP   │ │  Skill    │
       │ry        │ │Executor  │ │sion  │ │tionMgr  │ │Manager │ │  Catalog  │
       │(工具注册) │ │(并发执行) │ │(9层) │ │(对话管理)│ │(工具扩展)│ │ (技能系统) │
       ──────────┘ └──────────┘ ──────┘ └─────────┘ └────────┘ └───────────┘
```

### 2.2 核心设计原则

**三模式统一内核：** 同一个 Agent 核心服务 CLI/打印、交互式 TUI、Web/远程三种场景。UI 层完全可替换，Agent 通过 `BlockingQueue<AgentEvent>` 与 UI 层通信，自身不感知 UI 存在。

**事件驱动解耦：** Agent 循环运行在独立虚拟线程上，通过密封接口 `AgentEvent` 向 UI 层发射事件。TUI 和 Remote 共享同一套事件协议，UI 实现可以独立演进。

**虚拟线程并发：** Agent 循环、MCP 连接、工具执行、远程消息处理全部运行在 Java 21 虚拟线程上。`StreamingExecutor` 使用 `Executors.newVirtualThreadPerTaskExecutor()` 实现读操作的并行执行。

**纵深安全防御：** 权限系统包含 9 层独立检查，从安全命令白名单到危险模式拦截，再到 YAML 规则引擎和 OS 级沙箱，任一层拒绝即终止操作。

**协议适配抽象：** `LlmClient` 接口统一 Anthropic 和 OpenAI 双协议为同一套内部事件接口（`StreamEvent` 密封接口），新增 LLM 供应商只需实现一个接口。

**延迟工具加载：** 工具可声明 `shouldDefer() = true`，其 Schema 不发送给 LLM，直到通过 `ToolSearch` 显式发现。MCP 工具全部默认延迟加载，百级工具场景下工具描述 Token 占用减少 85%。

---

## 三、包结构总览

| 包名 | 文件数 | 职责 |
|------|--------|------|
| `com.magiccode` | 1 | 入口类 `MagicCode`，解析 CLI 参数并分发到三种模式 |
| `com.magiccode.agent` | 3 | 核心 Agent 循环（`Agent`）、事件协议（`AgentEvent` 密封接口）、并发工具执行器（`StreamingExecutor`） |
| `com.magiccode.command` | 3 | 斜杠命令系统（`Command`、`CommandRegistry`、`CommandLoader`） |
| `com.magiccode.compact` | 2 | 上下文窗口管理：两层压缩（工具结果溢出 + LLM 摘要），恢复附件，PTL 重试 |
| `com.magiccode.config` | 6 | 配置加载（三层合并），`AppConfig`、`ProviderConfig`、`HookConfig`、`McpServerConfig`、`SandboxYamlConfig` |
| `com.magiccode.conversation` | 4 | 对话状态管理（`ConversationManager`、`Message`、`ThinkingBlock`、`ToolUseBlock`、`ToolResultBlock`） |
| `com.magiccode.filehistory` | 1 | 文件版本快照与回退（`FileHistory`），基于 SHA-256 哈希命名备份 |
| `com.magiccode.history` | 1 | 用户输入历史持久化（`HistoryStore`，JSONL 循环缓冲） |
| `com.magiccode.hook` | 1 | 生命周期钩子引擎（`HookEngine`）：9 种事件、4 种动作、条件求值、once/async/onError |
| `com.magiccode.llm` | 7 | LLM 客户端：`AnthropicClient`、`OpenAiClient`、`OpenAiCompatClient`，流式事件（`StreamEvent` 密封接口），工厂类，异常体系 |
| `com.magiccode.mcp` | 1 | MCP 集成（`McpManager`）：stdio/HTTP 传输、工具包装、环境变量解析、Windows 兼容 |
| `com.magiccode.memory` | 4 | 持久化记忆系统（`MemoryManager`）：Markdown 文件 + YAML 前置元数据，LLM 提取、索引生成、记忆召回 |
| `com.magiccode.permission` | 3 | 权限系统（`PermissionChecker`）：9 层检查流水线，YAML 规则引擎，OS 沙箱联动 |
| `com.magiccode.plan` | 1 | Plan 模式支持（`PlanFile`），在 `.magiccode/plans/` 下创建计划文件 |
| `com.magiccode.print` | 1 | 非交互打印模式（`PrintMode`），支持 TEXT 和 STREAM_JSON 输出格式 |
| `com.magiccode.prompt` | 3 | 系统提示词构建（`PromptBuilder`、`PromptSections`），按优先级组装各段落 |
| `com.magiccode.remote` | 2 | 远程 Web 模式（`RemoteServer`）：Spring Boot HTTP + WebSocket，内嵌 index.html |
| `com.magiccode.sandbox` | 4 | OS 级沙箱：macOS Seatbelt、Linux Bubblewrap，`SandboxFactory` 按系统选择 |
| `com.magiccode.session` | 1 | 会话持久化（`SessionManager`），JSONL 格式存储，支持压缩感知恢复 |
| `com.magiccode.skill` | 7 | 技能系统（`SkillCatalog`、`SkillExecutor`、`SkillInstaller`），inline/fork 双模式 |
| `com.magiccode.subagent` | 5 | 子 Agent 管理（`AgentTool`、`SubAgentTaskManager`），fork/spec/teammate 三种派发路径 |
| `com.magiccode.task` | 2 | 任务/待办管理（`TaskList`、`TaskTools`），JSON 持久化 |
| `com.magiccode.teams` | 10 | 多 Agent 团队协作（`TeamManager`、`FileMailBox`、`Coordinator`），IN_PROCESS/TMUX/ITERM 后端 |
| `com.magiccode.tool` | 4 | 工具框架（`Tool` 接口、`ToolRegistry`、`ToolResult`、`ToolCategory`） |
| `com.magiccode.tool.impl` | 12 | 12 个内置工具实现 |
| `com.magiccode.toolresult` | 5 | 工具结果预算管理（`ToolResultBudget`），大结果溢出到磁盘，内容替换状态追踪 |
| `com.magiccode.tui` | 6 | TUI 模型（`MagicCodeModel`），Markdown 渲染，样式系统，Spinner 动画 |
| `com.magiccode.tui.dialog` | 2 | TUI 对话框（`AskUserDialog`、`PlanApprovalDialog`） |
| `com.magiccode.tui.tea` | 11 | 自研 TEA 框架（`Program`/`Model`/`Command`/`Message`），ANSI 256 色，键鼠事件 |
| `com.magiccode.worktree` | 6 | Git Worktree 管理（`WorktreeManager`、`AgentWorktree`），并行 Agent 文件系统隔离 |

---

## 四、三种运行模式

### 4.1 TUI 模式（默认）

启动后进入交互式终端界面。基于自研 TEA（The Elm Architecture）框架，使用 JLine 做终端输入、Mordant 做颜色渲染。

**核心特性：** Provider 选择、聊天输入、斜杠命令、@文件引用、权限弹窗、回退（rewind）、Plan 审批、滚动历史、子 Agent 进度展示、团队协作面板。

```bash
java -jar target/magiccode-1.0.0.jar
```

> 注意：TUI 模式依赖终端控制序列，在 IDEA 的 Run 窗口中可能显示异常，建议在 IDEA 内置 Terminal 或外部终端中运行。

### 4.2 Print 模式（非交互）

单次执行，将结果输出到 stdout。适合脚本集成和 CI/CD 场景。

```bash
java -jar target/magiccode-1.0.0.jar -p "解释这个项目的架构"
java -jar target/magiccode-1.0.0.jar -p "重构 main 函数" --output-format stream-json
```

### 4.3 Remote 模式（Web 服务）

启动 Spring Boot HTTP + WebSocket 服务器，默认监听 `:18888`。浏览器访问 `http://localhost:18888` 即可使用 Web UI。

```bash
java -jar target/magiccode-1.0.0.jar --remote
java -jar target/magiccode-1.0.0.jar --remote=:9090   # 自定义端口
```

---

## 五、核心模块详解

### 5.1 Agent 循环（Agentic Loop）

`Agent.java` 是整个系统的核心引擎，实现了完整的 Agentic Loop：

**循环流程：**

1. **初始化阶段：** 注入长期记忆（指令 + 记忆内容）到对话中
2. **迭代循环：** 进入无界 `for` 循环（受 `maxIterations` 限制）
   - 排出后台任务通知并注入为系统提醒
   - 计算工具 Schema，应用可选的名称过滤器（子 Agent 场景）
   - 注入延迟工具名称（通过 `ToolSearch` 懒加载）
   - Plan 模式下注入结构化工作流提醒
   - **Layer 1：** 工具结果预算（截断旧的工具结果以节省 Token）
   - **Layer 2：** 自动压缩检查（对话过大时生成摘要）
   - 调用 `client.stream()` 获取 `BlockingQueue<StreamEvent>` 并消费
   - 错误恢复：上下文过长触发强制压缩（最多 3 次重试），速率限制触发 5 秒退避
   - `max_tokens` 停止原因处理：首次提升到 64K 输出上限，之后最多 3 次"续写"恢复
   - 无工具调用时发出 `LoopComplete` 并退出
   - 否则委托 `StreamingExecutor` 执行工具，将结果追加到对话，继续循环

**关键技术细节：**

- **UsageAnchor 机制：** 每次流结束后记录真实 Token 用量作为锚点，校准压缩器的字符估算
- **RecoveryState：** 维护文件读取快照，压缩后可恢复关键上下文
- **非阻塞记忆召回：** `CompletableFuture<String>` 与主 LLM 调用并行预取，第一轮工具执行后消费
- **内容替换状态：** 追踪每次对话的工具结果决策，维护字节稳定的前缀以支持 Anthropic 提示缓存

### 5.2 LLM 协议适配层

**`LlmClient` 接口** 定义了统一的 LLM 通信抽象：

```java
interface LlmClient {
    BlockingQueue<StreamEvent> stream(ConversationManager, List<Map<String, Object>> tools);
    void setMaxOutputTokens(int tokens);
    void setSystemPrompt(String prompt);
    static LlmClient create(ProviderConfig cfg, String workDir) { ... }
}
```

**`StreamEvent` 密封接口** 统一了所有 LLM 流式事件：

| 事件类型 | 用途 |
|---------|------|
| `TextDelta` | LLM 增量文本输出 |
| `ThinkingDelta` | 增量思考/推理文本 |
| `ThinkingComplete` | 最终思考块（含签名或推理 ID） |
| `ToolCallStart` | 新工具调用开始 |
| `ToolCallDelta` | 工具调用 JSON 参数增量片段 |
| `ToolCallComplete` | 工具调用参数完整接收 |
| `StreamEnd` | 流结束，携带停止原因和 Token 用量 |
| `Error` | 流式过程中的错误 |

**AnthropicClient 实现要点：**

- 使用官方 `AnthropicOkHttpClient` SDK，在虚拟线程上流式处理
- **三级提示缓存锚点：** 系统提示块、最后一个工具 Schema、最后一条用户消息的尾部内容块，均标记 `CacheControlEphemeral`
- **思考模式支持：** 自适应思考（`ThinkingConfigAdaptive`）和固定预算思考（`ThinkingConfigEnabled`）
- **上下文窗口探测：** 通过 `GET /v1/models/{model}` 获取，5 秒超时，失败回退到内置表
- **用量追踪：** 从 `message_start` 和 `message_delta` 事件提取输入/输出/缓存读/缓存创建 Token

**OpenAiClient 实现要点：**

- 使用 OpenAI Responses API（新版 API），映射 `ResponseStreamEvent` 到 `StreamEvent`
- 推理摘要配置为 `ReasoningEffort.HIGH` + `Summary.DETAILED`
- 缓存 Token 从 `usage.inputTokensDetails().cachedTokens()` 提取

### 5.3 工具系统

**`Tool` 接口** 定义了工具契约：

```java
interface Tool {
    String name();
    String description();
    ToolCategory category();
    Map<String, Object> schema();
    ToolResult execute(Map<String, Object> args, ToolContext ctx);
    default boolean shouldDefer() { return false; }  // 延迟加载钩子
}
```

**`ToolRegistry` 延迟加载机制：**

1. 工具声明 `shouldDefer() = true` 后，其 Schema 不出现在 `getAllSchemas()` 中
2. LLM 通过 `ToolSearch` 工具按名称/描述子串搜索隐藏工具
3. 找到后调用 `markDiscovered(name)` 将其加入可见列表
4. MCP 工具全部默认延迟加载（`McpToolWrapper.shouldDefer() = true`）
5. 协议适配：`getAllSchemas(protocol)` 根据 Anthropic 或 OpenAI 格式转换 Schema

**12 个内置工具：**

| 工具名 | 类别 | 功能 | 延迟加载 |
|--------|------|------|---------|
| `Bash` | COMMAND | Shell 命令执行，超时 600s，OS 沙箱包裹 | 否 |
| `ReadFile` | READ | 文件读取（带行号），offset/limit 分页 | 否 |
| `WriteFile` | WRITE | 创建/覆写文件，自动创建父目录，强制写前读取 | 否 |
| `EditFile` | WRITE | 精确字符串替换，`old_string` 唯一性校验 | 否 |
| `Glob` | READ | 文件模式匹配，按修改时间排序 | 否 |
| `Grep` | READ | 正则内容搜索，`file:line:content` 格式 | 否 |
| `AskUserQuestion` | READ | 结构化多选问题（1-4 个问题，每个 2-4 选项） | 否 |
| `ToolSearch` | READ | 发现延迟加载的工具 | 否 |
| `EnterWorktree` | COMMAND | 创建 Git Worktree 并切换会话 | 是 |
| `ExitWorktree` | COMMAND | 退出 Worktree 会话 | 是 |
| `ExitPlanMode` | READ | 结束 Plan 模式 | 是 |
| `InstallSkill` | WRITE | 从 URL 下载安装技能 | 是 |

**`StreamingExecutor` 并发执行策略：**

1. **分区：** 相邻的 READ 类工具合并为并行批次，WRITE/COMMAND 类工具各自独立顺序执行
2. **并行执行：** 使用 `Executors.newVirtualThreadPerTaskExecutor()` 在虚拟线程上并发执行读操作
3. **顺序执行：** 写/命令操作在调用虚拟线程上逐个执行，保证顺序性
4. **执行管道：** 权限检查 → 前置钩子 → 执行 → 恢复快照 → 输出截断 → 后置钩子

### 5.4 权限系统（9 层纵深防御）

`PermissionChecker` 实现 9 层检查流水线，按顺序评估：

| 层级 | 名称 | 说明 |
|------|------|------|
| Layer 0 | Plan 模式例外 | 仅允许 Agent、ToolSearch、AskUser、ExitPlanMode 和计划文件写入 |
| Layer 1 | 安全命令白名单 | `ls`、`cat`、`git status` 等只读命令自动放行（排除管道/分号/重定向） |
| Layer 2 | 危险命令检测 | 正则匹配 `rm -rf /`、`mkfs.`、`dd if=...of=/dev/`、fork bomb、`curl \| sh` 等，硬拒绝 |
| Layer 2b | 禁写路径保护 | 配置文件、权限文件、技能目录等受保护路径的写入一律拒绝 |
| Layer 3 | 路径沙箱 | 文件操作限制在项目根目录和 `/tmp` 范围内 |
| Layer 4 | YAML 规则引擎 | 三级规则文件（用户级、项目级、本地级），glob 模式匹配，最后匹配规则胜出 |
| Layer 4b | 会话级"始终允许"覆盖 | 用户点击"始终允许"后动态添加的规则 |
| Layer 4c | 沙箱自动放行 | OS 级沙箱激活时，命令工具自动通过（复合命令拆分逐条检查） |
| Layer 5 | 权限模式矩阵兜底 | 根据当前模式（DEFAULT/ACCEPT_EDITS/PLAN/BYPASS）和工具类别决定 |

**四种权限模式：**

| 模式 | READ | WRITE | COMMAND |
|------|------|-------|---------|
| DEFAULT | 允许 | 询问 | 询问 |
| ACCEPT_EDITS | 允许 | 允许 | 询问 |
| PLAN | 同 DEFAULT | 同 DEFAULT | 同 DEFAULT |
| BYPASS | 允许 | 允许 | 允许 |

**权限请求异步桥接：** `PermissionRequestEvent` 携带 `CompletableFuture<PermissionResponse>`，Agent 线程阻塞等待，UI 线程异步响应用户选择（ALLOW / ALLOW_ALWAYS / DENY），5 分钟超时。

### 5.5 上下文管理（两层渐进式压缩）

**Layer 1 — 工具结果溢出（`ToolResultBudget`）：**

- 单个结果超过 50,000 字符 → 溢出到 `.magiccode/tool_results/` 目录，替换为引用指针 + 2,000 字符预览
- 单条消息聚合超过 200,000 字符 → 从最大结果开始溢出，直到总量低于阈值
- **幂等决策：** `ContentReplacementState` 追踪 `seenIds` 和 `replacements`，后续调用复用缓存决策，确保字节稳定的输出以支持提示缓存
- **溢出回读检测：** `isSpillReadback()` 检测工具结果是否为读取之前溢出的文件，防止无限递归

**Layer 2 — LLM 摘要压缩（`ContextCompactor`）：**

- **触发阈值：** 基于绝对 Token 计数。`effectiveWindow = contextWindow - min(maxOutput, 20000)`。软触发在 `effectiveWindow - 13000`（自动），硬触发在 `effectiveWindow - 3000`（强制）
- **Token 估算：** 字符级启发式 3.5 chars/token，工具调用 +50 tokens，工具结果 +10 tokens
- **UsageAnchor 校准：** 真实 API 用量数据锚定基线，仅增量估算锚点之后的消息，避免缓存命中导致误判
- **保留窗口：** 从末尾向前遍历，保留最近 10,000-40,000 Token（至少 5 条消息），工具使用/结果对不被拆分
- **摘要生成：** 将旧消息发送给 LLM，要求生成 9 段结构化摘要（主要请求、技术概念、文件/代码、错误/修复、问题解决、用户消息、待办任务、当前工作、后续步骤）
- **PTL 重试：** 摘要请求本身超出上下文窗口时，丢弃最旧的 API 轮次组并重试（最多 3 次）
- **恢复附件：** 压缩后附加结构化恢复块（最近读取的文件快照、活跃技能 SOP、可用工具列表）
- **断路器：** `AutoCompactTrackingState` 连续 3 次失败后抑制后续自动压缩
- **会话持久化：** 写入 `compact_boundary` 记录到 JSONL 会话文件，支持从压缩状态恢复

### 5.6 多 Agent 协作

**子 Agent（`AgentTool`）三种派发路径：**

| 路径 | 触发条件 | 行为 |
|------|---------|------|
| **Fork** | 无 `subagent_type` | 克隆父对话历史，后台虚拟线程运行，防递归（检测 `<fork_boilerplate>` 标签） |
| **Spec** | 指定 `subagent_type` | 创建专用 Agent（general-purpose/explore/plan），过滤工具集，支持同步/异步 |
| **Teammate** | 指定 `team_name` | 启动长驻团队成员，通过 `TeamManager` 管理 |

**Worktree 隔离：** `isolation: "worktree"` 时创建临时 Git Worktree，子 Agent 在独立文件系统中工作，完成后自动清理（有未提交变更时保留）。

**团队协作（`TeamManager`）：**

- 三种执行后端：`IN_PROCESS`（同 JVM 虚拟线程）、`TMUX`（独立 tmux 面板）、`ITERM`（iTerm2 标签页）
- 后端自动检测：通过 `$TMUX` 和 `$ITERM_SESSION_ID` 环境变量自动选择
- **协调者模式（`Coordinator`）：** 限制 Lead Agent 仅使用协调工具集（Agent、SendMessage、TaskCreate/Get/List/Update、TeamCreate/Delete、ReadFile、Glob、Grep、Bash），遵循四阶段工作流：Research → Synthesis → Implementation → Verification

**Agent 间通信（`FileMailBox`）：**

- 基于文件的消息队列，每个 Agent 拥有 JSON 格式收件箱（`.magiccode/teams/<team>/inboxes/<agent>.json`）
- 锁文件实现并发安全（最多 10 次重试，5-100ms 随机间隔，10s 过期自动清理）
- 消息持久化到磁盘，进程重启后可恢复

**后台任务管理（`SubAgentTaskManager`）：**

- 任务状态机：PENDING → RUNNING → COMPLETED / FAILED / CANCELLED
- 父 Agent 每轮开始时排出通知（`drainNotifications()`），获取后台任务结果
- 所有状态变更 `synchronized` 保护

### 5.7 技能系统（Skill System）

**三层加载：** 内置技能 → `~/.magiccode/skills/`（用户级）→ `<workDir>/.magiccode/skills/`（项目级）

**双格式支持：** `skill.yaml` + `prompt.md`（分离式）或 `SKILL.md` 带 YAML 前置元数据（单文件式）

**双执行模式：**

| 模式 | 行为 |
|------|------|
| **inline** | 将技能 prompt 注入当前对话的系统提示中，替换 `$ARGUMENTS` 占位符 |
| **fork** | 启动子 Agent 执行技能，根据 `forkContext`（none/recent/full）传递父对话上下文 |

**热重载检测：** `needsReload()` 比较目录修改时间与快照，任何技能目录变更触发重新加载。

**远程安装（`SkillInstaller`）：** 从 GitHub/skills.sh 下载，支持三种 URL 格式，安全限制（单文件 1MB、总量 8MB、最多 64 文件、递归深度 4），路径遍历防护，先下载到临时目录验证后再原子移动。

### 5.8 记忆系统（Memory System）

**文件结构：** 每个记忆是一个独立的 `.md` 文件，带 YAML 前置元数据（name、description、metadata.type）。每个目录有 `MEMORY.md` 索引文件。

**两级存储：**

| 级别 | 路径 | 类型 |
|------|------|------|
| 用户级 | `~/.magiccode/memory/` | user、feedback |
| 项目级 | `<workDir>/.magiccode/memory/` | project、reference |

**LLM 驱动提取：** 每 5 轮对话（`EXTRACTION_INTERVAL`），将最近 40 条消息发送给 LLM，要求按结构化格式（MEMORY_NAME/TYPE/DESC/BODY）提取记忆，同时提供已有记忆清单用于去重。

**注入机制：** 对话开始时，`MEMORY.md` 索引内容作为 `<system-reminder>` 注入，让 LLM 知道存在哪些记忆。

### 5.9 钩子引擎（Hook Engine）

**9 种生命周期事件：** SESSION_START、SESSION_END、TURN_START、TURN_END、PRE_SEND、POST_RECEIVE、PRE_TOOL_USE、POST_TOOL_USE、SHUTDOWN

**4 种动作类型：**

| 类型 | 行为 |
|------|------|
| COMMAND | 通过 `bash -c` 执行，注入环境变量（MAGICCODE_EVENT、MAGICCODE_TOOL、MAGICCODE_FILE_PATH），默认 10 分钟超时 |
| PROMPT | 返回消息字符串，用于注入文本到对话 |
| HTTP | 发送 HTTP 请求（默认 POST，自动 JSON 载荷），响应上限 64KB |
| AGENT | 委托给注册的 `agentRunner` BiFunction |

**条件表达式语言：** 支持 `==`（等于）、`!=`（不等于）、`=~`（正则匹配）、`=*`（glob 匹配）、`&&`、`||`、`!`（否定），变量包括 `tool`、`event`、`file_path`、`message`、`args.xxx`。

**特殊语义：** `once`（每个 Hook ID 仅触发一次）、`async`（`CompletableFuture.runAsync` 异步执行）、`reject`（阻止工具执行）、`onError=reject`（动作失败时阻止）。

### 5.10 TUI 渲染引擎

**自研 TEA 框架：** 实现了 The Elm Architecture 的 `init/update/view` 模式：

- `Model` 接口：`init()` → `Command`，`update(Message)` → `UpdateResult`，`view()` → `String`
- `Command` 密封接口：`Simple`、`Tick`、`CheckWindowSize`、`Batch`、`PrintLine`
- `Message` 标记接口：按键、鼠标、窗口大小、退出、Agent 事件等

**`Program` 运行时引擎：**

- 维护 `BlockingQueue<Message>` 事件总线
- 主循环：poll 消息（16ms 超时）→ `model.update(msg)` → 执行 `Command` → `renderView()`
- 虚拟线程 `keyReaderLoop` 非阻塞解析输入（CSI、SS3、SGR 鼠标、IME 感知 Unicode）

**绝对定位渲染策略：**

- 启动时发送 `\033[6n`（DSR）查询光标行号，存储为 `viewStartRow`
- 每次重绘使用 `\033[row;1H` 绝对定位，不使用相对光标上移（避免 Windows IME 光标跳变导致的状态损坏）
- 每行末尾 `\033[K` 清除残余字符，全部绘制后 `\033[J` 清除下方多余行
- Windows IME 修复：定位输入行（搜索 `❯` 提示符），将光标绝对定位到 prompt 之后

**混合滚动模型：** 已完成的对话内容通过 `Command.println` 提交到终端滚动缓冲区（永久，不再重绘），只有进行中的内容（流式文本、加载中的工具、输入区）在 TUI 视图中使用绝对定位重绘。

**物理行计算：** `physicalLineCount()` 考虑终端宽度换行和 CJK 全角字符（`isWide()` 覆盖统一 CJK 范围），确保渲染器精确知道逻辑视图占用多少物理终端行。

### 5.11 远程 Web 服务

**`RemoteServer` 架构：**

- Spring Boot 内嵌 Tomcat，HTTP + WebSocket 双协议
- WebSocket 端点 `/ws`，静态页面 `/`（内嵌 index.html）
- JSON 消息协议：客户端→服务器（user_message、permission_response、ask_user_response、cancel、ping），服务器→客户端（stream_text、thinking_text、tool_use、tool_result、permission_request 等 18 种事件类型）

**权限桥接：** `CompletableFuture<PermissionResponse>` 存储在 `pendingPerms` 映射表中，前端发送用户决策后完成 Future，解除 Agent 线程阻塞。

**会话管理：** 消息保存到 `SessionManager`，支持 `/resume` 恢复历史会话（压缩感知重放）。

### 5.12 Git Worktree 管理

**`WorktreeManager`：**

- 创建：`git worktree add -B <branch> <dir>`，然后执行 `PostCreationSetup` 创建依赖目录符号链接（如 `node_modules`）
- 清理：`cleanupStale(cutoffHours)` 移除超过阈值的 worktree，`removeAll()` 尽力清理所有
- 变更检测：`git diff --stat` 检查未提交变更
- 安全：`SlugValidator` 验证分支名安全性，所有 git 命令 60 秒超时
- 存储路径：`.magiccode/worktrees/<branch>/`

---

## 六、配置系统

### 6.1 三层配置合并

| 优先级 | 路径 | 用途 |
|--------|------|------|
| 1（基础） | `~/.magiccode/config.yaml` | 用户全局配置 |
| 2（覆盖） | `$CWD/.magiccode/config.yaml` | 项目级配置 |
| 3（覆盖） | `$CWD/.magiccode/config.local.yaml` | 本地/私密覆盖 |

合并策略：Provider 整体替换，MCP Server 按名称合并（同名覆盖、新增追加），Hook 追加，Sandbox 设置 OR 合并。

### 6.2 配置文件示例

```yaml
providers:
  - name: anthropic
    protocol: anthropic
    base_url: https://api.anthropic.com
    model: claude-3-5-sonnet-20241022
    api_key: ""                          # 留空则读取环境变量 ANTHROPIC_API_KEY

  - name: openai
    protocol: openai
    base_url: https://api.openai.com
    model: gpt-4o
    api_key: ""                          # 留空则读取环境变量 OPENAI_API_KEY

permission_mode: default

mcp_servers: []

hooks: []

sandbox:
  enabled: false
  auto_allow: true
  network_enabled: false

enable_coordinator_mode: false
```

### 6.3 环境变量

| 变量 | 用途 |
|------|------|
| `MAGICCODE_CONFIG` | 配置文件路径（CLI 参数的回退） |
| `ANTHROPIC_API_KEY` | Anthropic API 密钥 |
| `OPENAI_API_KEY` | OpenAI API 密钥 |
| `SHELL` | 系统提示词中检测用户 Shell |
| `TMUX` | 自动检测 tmux 环境，选择团队后端 |
| `ITERM_SESSION_ID` | 自动检测 iTerm2 环境 |

### 6.4 运行时目录结构

```
~/.magiccode/                              # 用户全局目录
├── config.yaml                            # 全局配置
├── MAGICCODE.md                           # 全局指令
├── AGENTS.md                              # 全局 Agent 定义
├── memory/                                # 用户级记忆文件
│   ├── MEMORY.md                          # 记忆索引
│   └── *.md                               # 独立记忆文件
├── skills/                                # 已安装技能
├── commands/                              # 自定义斜杠命令
└── permissions.yaml                       # 用户级权限规则

$CWD/.magiccode/                           # 项目级目录
├── config.yaml                            # 项目配置
├── config.local.yaml                      # 本地覆盖
├── permissions.yaml                       # 项目权限规则
├── permissions.local.yaml                 # 本地权限覆盖
── MAGICCODE.md                           # 项目指令
├── MAGICCODE.local.md                     # 本地指令覆盖
├── AGENTS.md                              # 项目 Agent 定义
├── sessions/                              # 会话 JSONL 文件
├── plans/                                 # Plan 模式计划文件
── memory/                                # 项目级记忆
── skills/                                # 项目级技能
├── teams/                                 # 团队协作数据（邮箱）
│   └── <team>/inboxes/<agent>.json
├── worktrees/                             # Git Worktree
├── tool_results/                          # 溢出的工具结果
── file-history/                          # 文件历史备份（SHA-256 命名）
└── commands/                              # 项目级自定义命令
```

---

## 七、关键设计模式与 Java 21 特性应用

### 7.1 密封接口 + Record（类型安全的事件协议）

`AgentEvent`、`StreamEvent`、`Command` 等使用 Java 密封接口，配合 Record 类型实现穷举模式匹配：

```java
sealed interface AgentEvent permits
    StreamText, ThinkingText, ToolUseEvent, ToolResultEvent,
    TurnComplete, LoopComplete, UsageEvent, ErrorEvent, ... {}

// Agent 循环中的穷举匹配
switch (event) {
    case StreamText t -> handleText(t);
    case ToolUseEvent t -> handleToolUse(t);
    case LoopComplete l -> { running = false; }
    // 编译器确保所有类型都被处理
}
```

### 7.2 虚拟线程并发（Virtual Threads）

全系统并发策略：

| 场景 | 并发方式 |
|------|---------|
| Agent 主循环 | `Thread.startVirtualThread(this::agentLoop)` |
| LLM 流式调用 | `Thread.startVirtualThread(() -> doStream(...))` |
| 读工具并行执行 | `Executors.newVirtualThreadPerTaskExecutor()` |
| 子 Agent 后台任务 | `Thread.startVirtualThread(() -> runSubAgent(...))` |
| 记忆召回预取 | `Thread.startVirtualThread(() -> recallMemories(...))` |
| Hook 异步执行 | `CompletableFuture.runAsync(...)` |
| TUI 定时器 | `Thread.startVirtualThread(() -> { Thread.sleep(...); })` |

### 7.3 CompletableFuture 异步桥接

权限请求和用户提问通过 `CompletableFuture` 实现 Agent 线程阻塞等待、UI 线程异步响应的桥接模式：

```java
// Agent 线程
CompletableFuture<PermissionResponse> future = new CompletableFuture<>();
msgQueue.put(new PermissionRequestEvent(description, future));
PermissionResponse response = future.get(5, TimeUnit.MINUTES);  // 阻塞等待

// UI 线程
var event = (PermissionRequestEvent) msg;
event.future().complete(userChoice);  // 异步完成
```

### 7.4 BlockingQueue 事件总线

所有跨线程通信通过有界 `LinkedBlockingQueue`（容量 64）实现，提供自然背压：

- Agent → UI：`BlockingQueue<AgentEvent>`
- LLM → Agent：`BlockingQueue<StreamEvent>`
- Program → Model：`BlockingQueue<Message>`

### 7.5 策略模式（多 Provider 适配）

`LlmClient` 接口 + 静态工厂方法，不同 LLM Provider 可互换：

```java
static LlmClient create(ProviderConfig cfg, String workDir) {
    return switch (cfg.getProtocol()) {
        case "anthropic" -> new AnthropicClient(cfg, workDir);
        case "openai" -> new OpenAiClient(cfg);
        case "openai-compat" -> new OpenAiCompatClient(cfg);
        default -> throw new IllegalArgumentException("Unknown protocol: " + cfg.getProtocol());
    };
}
```

### 7.6 状态模式（权限模式）

`PermissionMode` 枚举封装决策矩阵，`decide(ToolCategory)` 方法将模式状态映射为具体决策。

---

## 八、构建与测试

### 8.1 构建命令

```bash
# 编译
mvn compile

# 运行测试（104 个测试用例）
mvn test

# 打包 Fat JAR
mvn package -DskipTests

# 运行
java -jar target/magiccode-1.0.0.jar
```

### 8.2 构建状态

- 编译：133 个源文件，0 错误
- 测试：104 个测试，0 失败，1 跳过（Windows 环境下 bash 内置命令不可用，自动跳过）

### 8.3 测试覆盖

| 测试文件 | 测试数 | 覆盖模块 |
|---------|--------|---------|
| ContextCompactorTest | 16 | 上下文压缩（触发阈值、保留窗口、PTL 重试、恢复附件） |
| RecoveryAttachmentTest | 5 | 压缩后恢复附件（文件快照、技能 SOP、工具列表） |
| ProviderConfigContextWindowTest | 8 | Provider 上下文窗口配置 |
| HookEngineTest | 13 | 钩子引擎（条件求值、超时、环境变量、模板替换） |
| AnthropicClientContextWindowTest | 4 | Anthropic 客户端上下文处理 |
| UsageParsingTest | 5 | Token 用量解析 |
| PermissionCheckerTest | 3 | 权限检查（多层流水线） |
| SessionManagerTest | 8 | 会话管理（创建、追加、恢复、过期清理） |
| SkillInstallerTest | 20 | 技能安装（URL 解析、安全限制、文件提取） |
| FileMailBoxTest | 5 | Agent 间邮箱通信（并发安全、锁超时） |
| ToolSearchTest | 6 | 延迟工具搜索（发现、过滤、协议适配） |
| ContentReplacementStateTest | 2 | 内容替换状态（幂等决策） |
| ReplacementRecordsIOTest | 2 | 替换记录持久化 |
| ToolResultBudgetTest | 7 | 工具结果预算（单结果溢出、聚合溢出、回读检测） |

---

## 九、IntelliJ IDEA 启动指南

### 9.1 导入项目

IDEA 中选择 File → Open，直接打开项目目录。IDEA 会自动识别 `pom.xml` 并导入 Maven 项目，等待依赖下载完成。

### 9.2 配置 JDK 21

File → Project Structure → Project，确认 SDK 选择 Java 21 或以上，Language level 设为 21。Maven 的 Importer 和 Runner 中的 JRE 也要对应 21。

### 9.3 准备配置文件

在项目根目录创建 `.magiccode/config.yaml`，内容参考第六节示例。API Key 推荐通过环境变量传入。

### 9.4 创建 Run Configuration

| 配置项 | 值 |
|--------|---|
| Name | MagicCode |
| Main class | `com.magiccode.MagicCode` |
| Working directory | 项目根目录（或目标工作目录） |
| JRE | Java 21 |
| VM options | `-Dfile.encoding=UTF-8` |

### 9.5 三种启动方式

- **TUI 模式：** Program arguments 留空
- **Print 模式：** Program arguments 填 `-p "你的提示词"`
- **Remote 模式：** Program arguments 填 `--remote`

---

## 十、技术亮点与面试题

以下基于本项目实际实现的技术亮点，整理了面试中可能被问到的问题及参考答案思路。

---

### 10.1 MCP 工具延迟加载

**背景：** MCP（Model Context Protocol）服务器可能暴露数十甚至上百个工具，如果将所有工具的 Schema 描述都放入系统提示词，会严重挤占 LLM 的上下文窗口。

**设计：** 所有 MCP 工具默认声明 `shouldDefer() = true`，其 Schema 不发送给 LLM。LLM 通过内置的 `ToolSearch` 工具按名称/描述子串搜索隐藏工具，找到后调用 `markDiscovered()` 将其加入可见列表。

**效果：** 百级工具场景下，工具描述 Token 占用减少约 85%。

**面试题：**

> **Q：你们项目有上百个工具，怎么避免工具描述挤占 LLM 的上下文窗口？**
>
> **A：** 我们设计了延迟加载机制。工具接口有一个 `shouldDefer()` 方法，返回 true 的工具其 Schema 不会出现在发给 LLM 的工具列表中。LLM 只能通过一个叫 `ToolSearch` 的内置工具来搜索和发现这些隐藏工具。发现后调用 `markDiscovered()` 才将其加入可见列表。MCP 工具全部默认延迟加载。这样初始 Token 占用可以减少 85% 以上。核心思路是把"全量推送"改为"按需发现"。

> **Q：延迟加载会不会导致 LLM 不知道有哪些工具可用？**
>
> **A：** 不会。`ToolSearch` 工具本身始终可见，LLM 在需要时可以主动搜索。同时我们在系统提示词中说明了"你可以使用 ToolSearch 来发现更多工具"。实践表明 LLM 能很好地理解和使用这个模式。

---

### 10.2 多 LLM 协议适配

**背景：** 项目需要同时支持 Anthropic 和 OpenAI 两种 API 协议，两者的流式事件格式、工具调用格式、用量统计方式完全不同。

**设计：** 定义 `LlmClient` 接口和 `StreamEvent` 密封接口作为统一抽象层。`AnthropicClient` 和 `OpenAiClient` 各自适配 SDK 的事件模型，输出统一的 `StreamEvent`。Agent 核心完全不感知底层 Provider。

**效果：** 新增 LLM 供应商只需实现 `LlmClient` 接口，Agent 核心零修改。

**面试题：**

> **Q：你们怎么同时支持 Anthropic 和 OpenAI 的 API？两套协议差异很大。**
>
> **A：** 我们定义了一个 `LlmClient` 接口和一个 `StreamEvent` 密封接口作为统一抽象。Anthropic 的 `RawMessageStreamEvent` 和 OpenAI 的 `ResponseStreamEvent` 各自通过适配器转换为 `StreamEvent`。Agent 核心只看到 `StreamEvent`，完全不知道底层是哪个 Provider。新增供应商只需要实现 `LlmClient` 接口，Agent 代码一行都不用改。这就是典型的适配器模式 + 策略模式。

> **Q：`StreamEvent` 为什么用密封接口而不是普通接口？**
>
> **A：** 密封接口配合 Record 可以实现穷举模式匹配。Agent 循环中的 `switch` 语句可以覆盖所有事件类型，编译器会检查是否有遗漏。如果新增事件类型，编译就会报错，强制开发者处理。这比运行时抛 `ClassCastException` 安全得多。

---

### 10.3 两层渐进式上下文压缩

**背景：** LLM 有固定的上下文窗口限制，长时间编程会话可能产生大量工具结果和对话历史，超出窗口会导致 API 报错。

**设计：**

- **Layer 1（工具结果溢出）：** 单个结果超 50,000 字符或消息聚合超 200,000 字符时，溢出到磁盘文件，替换为引用指针 + 预览。幂等决策确保提示缓存稳定。
- **Layer 2（LLM 摘要压缩）：** 基于绝对 Token 计数触发，保留最近 10K-40K Token 原文，将旧消息发送给 LLM 生成 9 段结构化摘要。压缩后附加恢复附件（文件快照、技能 SOP、工具列表）。UsageAnchor 机制用真实 API 用量校准估算。

**效果：** 支持数小时连续编程会话不丢失关键上下文。

**面试题：**

> **Q：长时间对话超出 LLM 上下文窗口怎么办？**
>
> **A：** 我们设计了两层渐进式压缩。第一层是工具结果溢出，单个结果太大就写到磁盘文件，对话里只留引用和预览。第二层是 LLM 摘要压缩，当 Token 接近窗口上限时，保留最近的对话原文，把旧消息发给 LLM 生成结构化摘要。压缩后还会附加恢复附件，包括最近读过的文件快照和活跃技能，帮 LLM 保持上下文连续性。这样数小时的会话也能正常运行。

> **Q：压缩后 LLM 怎么知道之前读过哪些文件？**
>
> **A：** 我们有一个 `RecoveryState` 机制。每次 ReadFile 工具执行时，会重新从磁盘读取文件内容作为快照保存。压缩发生后，这些快照会作为"恢复附件"附加在摘要后面。LLM 看到摘要的同时也能看到关键文件的内容，不需要重新读取。

> **Q：怎么判断什么时候该触发压缩？**
>
> **A：** 我们不用比例，用绝对 Token 计数。先计算有效窗口（总窗口减去预留输出空间），然后在有效窗口减去 13,000 Token 时软触发自动压缩，减去 3,000 Token 时强制压缩。Token 估算用字符数除以 3.5，再用真实 API 返回的用量数据做锚点校准，避免缓存命中导致估算偏高。

---

### 10.4 五层（九层）权限拦截

**背景：** AI 编程助手需要执行 Shell 命令和文件操作，如果权限控制不当可能导致数据丢失或安全漏洞。

**设计：** 9 层纵深防御流水线，从 Plan 模式例外、安全命令白名单、危险命令检测、禁写路径保护、路径沙箱、YAML 规则引擎、会话级覆盖、沙箱自动放行到权限模式矩阵兜底。任一层拒绝即终止操作。

**效果：** Agent 全自动模式下也能安全执行，用户可通过 YAML 规则精细控制。

**面试题：**

> **Q：AI 助手要执行 Shell 命令，你们怎么保证安全？**
>
> **A：** 我们设计了 9 层纵深防御。第一层是 Plan 模式只允许特定工具。第二层是安全命令白名单，ls、cat 这类只读命令直接放行。第三层是危险命令检测，rm -rf /、fork bomb 这类直接硬拒绝。还有禁写路径保护、路径沙箱限制在项目目录内、YAML 规则引擎让用户自定义规则、OS 沙箱联动等。每一层都可以覆盖前一层的结果，但任何一层拒绝都会终止操作。

> **Q：用户想自定义权限规则怎么办？**
>
> **A：** 我们有一个 YAML 规则引擎，支持三级规则文件：用户级、项目级、本地级。规则格式是 `工具名(glob模式) allow/deny/ask`，最后匹配的规则胜出。比如可以写 `Bash(rm *) deny` 来禁止所有 rm 命令。用户还可以在执行时选择"始终允许"，动态添加会话级规则。

---

### 10.5 虚拟线程高并发

**背景：** Agent 需要同时处理 LLM 流式响应、多个工具并行执行、后台子 Agent 任务、记忆召回等并发场景。

**设计：** 全系统使用 Java 21 虚拟线程。Agent 主循环、LLM 流式调用、工具并行执行（`Executors.newVirtualThreadPerTaskExecutor()`）、子 Agent 后台任务、记忆预取全部运行在虚拟线程上。

**效果：** 轻量级高并发，无需线程池调优，代码简洁。

**面试题：**

> **Q：你们项目哪些地方用到了并发？怎么管理的？**
>
> **A：** 我们全面使用了 Java 21 的虚拟线程。Agent 主循环在一个虚拟线程上运行，LLM 流式调用在另一个虚拟线程上，工具执行用 `newVirtualThreadPerTaskExecutor()` 实现读操作的并行执行。子 Agent 后台任务、记忆召回预取也都是虚拟线程。虚拟线程的好处是不需要调线程池参数，代码里直接 `Thread.startVirtualThread()` 就行，非常轻量。

> **Q：虚拟线程和平台线程有什么区别？为什么选虚拟线程？**
>
> **A：** 平台线程绑定 OS 线程，创建成本高，数量受限。虚拟线程是 JVM 调度的轻量级线程，创建成本几乎为零，可以轻松创建成千上万个。我们的场景是 I/O 密集型（LLM API 调用、文件读写、网络请求），虚拟线程在 I/O 阻塞时会自动让出载体线程，非常适合。如果用平台线程池，需要仔细调参，而虚拟线程完全不需要。

---

### 10.6 TUI 绝对定位渲染

**背景：** Windows 终端的 IME（中文输入法）会在输入时移动光标位置，传统的相对光标上移（`\033[nA`）渲染策略会因光标位置偏移而导致画面错乱。

**设计：** 启动时通过 `\033[6n`（DSR）查询光标行号作为 `viewStartRow`，每次重绘使用 `\033[row;1H` 绝对定位。渲染后定位输入行，将光标移到 prompt 之后。混合滚动模型将已完成内容提交到终端滚动缓冲区，只重绘进行中的内容。

**效果：** 解决了 Windows IME 中文输入时的光标定位问题，画面稳定不闪烁。

**面试题：**

> **Q：终端 TUI 在 Windows 上中文输入有问题，你们怎么解决的？**
>
> **A：** 核心问题是 Windows IME 会移动光标位置，导致基于相对光标上移的渲染策略失效。我们的解决方案是改用绝对定位。启动时用 `\033[6n` 查询当前光标行号作为基准，每次重绘都用 `\033[row;1H` 绝对定位到那个行，不再依赖相对移动。渲染完成后把光标定位到输入行的 prompt 之后，这样 IME 候选窗口就会出现在正确的位置。

> **Q：TUI 的滚动是怎么实现的？**
>
> **A：** 我们用了混合模型。已经完成的对话内容通过 `PrintLine` 命令提交到终端的滚动缓冲区，这部分是永久的，不再重绘。只有正在进行的内容（流式文本、加载中的工具、输入区）在 TUI 视图中用绝对定位重绘。这样既保留了终端原生的滚动能力，又保证了活动区域的精确控制。

---

### 10.7 事件驱动架构

**背景：** Agent 核心需要与多种 UI（TUI、Web、Print）交互，但不同 UI 的交互方式差异巨大。

**设计：** Agent 通过 `BlockingQueue<AgentEvent>` 发射事件，UI 层消费事件。权限请求和用户提问通过 `CompletableFuture` 实现跨线程异步桥接。Agent 核心完全不感知 UI 存在。

**效果：** TUI 和 Remote 共享同一套 Agent 核心，UI 层完全可替换。

**面试题：**

> **Q：Agent 和 UI 之间怎么通信的？**
>
> **A：** 我们用了事件驱动架构。Agent 运行在独立的虚拟线程上，通过 `BlockingQueue<AgentEvent>` 向 UI 层发射事件。UI 层（TUI 或 Web）消费这些事件来更新界面。对于需要用户反馈的场景（比如权限确认），事件里携带一个 `CompletableFuture`，Agent 线程阻塞等待，UI 线程在用户操作后完成这个 Future。这样 Agent 核心完全不知道 UI 的存在，TUI 和 Web 可以共享同一套 Agent 代码。

> **Q：为什么用 BlockingQueue 而不是其他通信方式？**
>
> **A：** BlockingQueue 天然提供背压。我们设了容量 64，如果 UI 消费慢了，Agent 会自动阻塞等待，不会无限堆积事件。而且它是线程安全的，不需要额外的同步机制。比用 Callback 或 Observer 模式更简单直接。

---

### 10.8 提示缓存优化

**背景：** Anthropic API 支持提示缓存（Prompt Caching），合理放置缓存锚点可以显著降低 API 成本和延迟。

**设计：** 三级缓存锚点策略：系统提示块标记 `CacheControlEphemeral`、最后一个工具 Schema 标记缓存（覆盖整个工具块）、最后一条用户消息的尾部内容块标记缓存。内容替换状态追踪确保字节稳定的前缀。

**效果：** 最大化缓存命中率，降低 API 调用成本和延迟。

**面试题：**

> **Q：你们怎么优化 LLM API 的成本和延迟？**
>
> **A：** 主要用了 Anthropic 的提示缓存功能。我们在三个位置放置缓存锚点：系统提示、工具 Schema 块、最后一条用户消息的尾部。这样每次对话时，这些不变的部分可以直接从缓存读取，不需要重新处理。为了保证缓存命中，我们还追踪了工具结果的替换决策，确保每次生成的提示字节完全一致。

---

### 10.9 多 Agent 协作架构

**背景：** 复杂任务需要多个 Agent 并行工作，每个 Agent 需要独立的上下文和文件系统。

**设计：** 子 Agent 支持 Fork（克隆对话）、Spec（专用类型）、Teammate（长驻团队）三种派发路径。Git Worktree 实现文件系统隔离。FileMailBox 实现基于文件的 Agent 间通信。Coordinator 模式限制 Lead Agent 仅做协调。

**效果：** 支持多 Agent 并行处理复杂任务，文件系统互不干扰。

**面试题：**

> **Q：多个 Agent 怎么协作？怎么避免文件冲突？**
>
> **A：** 我们用了 Git Worktree 做文件系统隔离。每个子 Agent 可以在独立的 Worktree 中工作，有自己的分支和工作目录，互不干扰。Agent 间通信通过 FileMailBox，基于文件的消息队列，每个 Agent 有独立的收件箱。Lead Agent 可以进入 Coordinator 模式，只做任务分解和协调，不直接操作文件，由团队成员执行具体任务。

---

### 10.10 技能系统（可复用过程知识）

**背景：** 不同的编程任务有不同的最佳实践和工作流程，硬编码在系统提示中不够灵活。

**设计：** 技能系统支持三层加载（内置/用户/项目）、双格式（YAML+Markdown 或单文件 SKILL.md）、双执行模式（inline 注入对话或 fork 子 Agent）、热重载检测、远程安装（GitHub/skills.sh）。

**效果：** 用户可以自定义和分享技能，LLM 按需激活相关技能的最佳实践。

**面试题：**

> **Q：你们怎么让 LLM 掌握不同领域的最佳实践？**
>
> **A：** 我们设计了一个技能系统。每个技能是一个 Markdown 文件，里面包含最佳实践和工作流程。技能可以三层加载：内置的、用户全局的、项目级的。执行时有两种模式：inline 模式把技能内容注入当前对话的系统提示，fork 模式启动一个子 Agent 专门按技能流程执行。LLM 可以根据任务自动选择合适的技能，用户也可以手动激活。

---

### 10.11 会话持久化与压缩感知恢复

**背景：** 用户可能关闭终端后重新打开，需要恢复之前的对话状态，包括已经压缩过的上下文。

**设计：** JSONL 格式的会话文件，每条消息一行。压缩时写入 `compact_boundary` 记录，包含摘要和保留的原文消息。恢复时扫描最后一个边界记录，重建压缩后的状态，无需重放完整的压缩前历史。

**效果：** 秒级恢复长会话，即使经过多次压缩也能正确重建上下文。

**面试题：**

> **Q：会话关闭后怎么恢复？特别是已经压缩过的对话？**
>
> **A：** 我们用 JSONL 格式存储会话，每行一条消息。压缩发生时，会写入一个 `compact_boundary` 记录，里面包含 LLM 生成的摘要和需要保留的原文消息。恢复时扫描最后一个边界记录，用摘要重建压缩前的上下文，再加上边界之后的新消息。这样不需要重放完整的压缩前历史，恢复速度很快。

---

### 10.12 钩子引擎（生命周期扩展点）

**背景：** 用户需要在 Agent 的不同生命周期阶段执行自定义操作，比如工具执行前做安全检查、对话结束后发送通知。

**设计：** 9 种生命周期事件、4 种动作类型（命令/Prompt/HTTP/Agent）、条件表达式语言（支持等于/正则/glob/逻辑运算）、once/async/reject/onError 语义。

**效果：** 用户可通过 YAML 配置灵活扩展 Agent 行为，无需修改代码。

**面试题：**

> **Q：用户想在工具执行前做自定义检查，你们怎么支持的？**
>
> **A：** 我们有一个钩子引擎，支持 9 种生命周期事件，包括 PRE_TOOL_USE。用户可以配置一个钩子，在工具执行前触发。钩子有四种动作类型：执行命令、返回文本、发 HTTP 请求、或委托给 Agent。还有条件表达式，可以按工具名、文件路径等做匹配。钩子还可以设置 reject 语义，直接阻止工具执行。全部通过 YAML 配置，不需要改代码。
